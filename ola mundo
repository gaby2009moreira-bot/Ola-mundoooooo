using System;

namespace OlaMundo
{ // inicio
    class Program
    {
        static void Main(string[] args) // método principal
        {
            // Console.WriteLine("Vai corinthianssssss!"); // comando escreva
            // Console.WriteLine("2 + 3 é: " + (2 + 3));
            // Console.WriteLine("2 - 3 é: " + (2 - 3));
            // Console.WriteLine("2 * 3 é: " + (2 * 3));
            // Console.WriteLine("2 / 3 é: " + (2 / 3));

            // declaração de variáveis
            // tipoDoDado nomeDaVariável
            // int (numérico inteiro -> 0)
            // double (numérico real -> 0.00) 
            // bool (valor lógico -> V/F)
            // char (uma unica letra -> 's')
            // string (cadeia de caracteres -> "Zanetti Gostoso!")

            // int n1 = 12;
            // int n2 = 12;

            // Console.WriteLine(n1 + n2);
            // Console.WriteLine(Math.PI);

            //declarar a var nome e idade
            //e apresentar: "Olá Gaby, você tem x anos!!"

            //string nome = "Gaby";
            //int idade = 17;


            //Console.WriteLine("Olá " + nome + ", você tem " + idade + " anos!!");

            //declarar UMA var númerica inteira e atribuir um valor qualquer
            //apresentar seu sucessor e seu antecessor

            //int n1 = 17;

            //Console.WriteLine("Número inteiro: 17");
            //Console.WriteLine("sucessor: " + (n1 + 1));
            //Console.WriteLine("antecessor: " + (n1 - 1));

            //declarar DUAS var numéricas inteiras e calcular:
            //a soma dos quadrados dos números 
            //o quadrado da soma dos números

            int n1 = 10;
            int n2 = 8;

            Console.WriteLine("Soma das variaveis:" + (n1 + n2));
            Console.WriteLine("Soma dos quadrados:" + (n1 * n1) + " e " + (n2 * n2) + " igual " + (n1 * n1 + n2 * n2));
            Console.WriteLine("Quadrados das somas dos números:" + (n1 * n1 + n2 * n2) * (n1 * n1 + n2 * n2));

        }

    }
} // fim
 
